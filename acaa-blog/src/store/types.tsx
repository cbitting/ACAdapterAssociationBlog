export interface BlogPost {
    id?: string;
    title: string;
    description: string;
    content: string;
  }

  export interface PostComment {
    id: string;
    name: string;
    comment: string;   
  }